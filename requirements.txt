# =============================================================================
# Mintzy Plugin (mintzy-plugin-updated) — final requirements
# Python: 3.10–3.12 recommended
# Install:  pip install -r requirements.txt
#
# Cross-checked against: api_server.py, session_manager.py,
# auto_trader*.py, broker_angle.py, client.py, live_ltp_ws.py, orderbook.py
# =============================================================================

# --- API server (FastAPI) ---
fastapi==0.116.1
uvicorn[standard]==0.35.0
starlette==0.47.1
pydantic==2.11.7
pydantic-core==2.33.2
python-multipart>=0.0.9
python-dotenv==1.1.1
PyJWT>=2.8.0,<3.0.0
cryptography>=46.0.0
httpx==0.28.1
anyio==4.9.0
sniffio==1.3.1
h11==0.16.0
click==8.2.1
typing-extensions==4.12.2

# --- MongoDB ---
pymongo==4.15.2
dnspython==2.8.0

# --- Redis (session workers, LTP cache, market client) ---
redis==7.4.0

# --- HTTP / networking ---
requests==2.32.4
urllib3==2.3.0
certifi==2025.7.9
charset-normalizer==3.4.2
idna==3.10
websocket-client==1.9.0
websockets==15.0.1

# --- Data / numerics (traders + clients) ---
numpy==1.26.4
pandas==2.3.1
python-dateutil==2.9.0.post0
pytz==2025.1
tzdata==2025.1

# --- Angel One broker (SmartAPI) ---
smartapi-python==1.5.5
pyotp==2.9.0
tabulate==0.9.0
logzero==1.7.0

# --- Process management (VM: gunicorn -w N api_server:app) ---
gunicorn>=22.0.0,<24.0.0
psutil==7.0.0

# =============================================================================
# OPTIONAL — only if you run prediction_service.py on THIS same host
# (Skip if predictions come from an external service, e.g. :8000/predict)
# Install with: pip install -r requirements-prediction.txt
# or uncomment the block below.
# =============================================================================
# tensorflow==2.19.0
# scikit-learn==1.7.0
# matplotlib==3.10.3
# joblib==1.5.1
# ta==0.11.0
# yfinance==0.2.65
# hmmlearn==0.3.3

# =============================================================================
# OPTIONAL — dev / legacy scripts (test_new.py, minting_client.py, etc.)
# =============================================================================
# Minting==2.1.6
# Mintzy==2.0.1
